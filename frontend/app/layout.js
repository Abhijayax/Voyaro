import './globals.css';
export const metadata = { title: 'Shimla Trip Planner', description: 'AI-powered itineraries for Shimla' };
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
